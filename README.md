# Spam-Ham-Classifier
This project implements a binary text classification model to distinguish between spam and ham (non-spam) messages. The model leverages Word2Vec embeddings to convert words into dense vector representations, capturing their semantic relationships.
